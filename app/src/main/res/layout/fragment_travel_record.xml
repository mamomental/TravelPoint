<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin">
    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@+id/travel_record_image"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        fresco:viewAspectRatio= "1.33"
        android:layout_alignParentTop="true"
        android:layout_above="@+id/travel_record_text"
        fresco:placeholderImage="@drawable/penguin" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="200dip"
        android:text="New Text"
        android:id="@+id/travel_record_text"
        android:layout_alignParentBottom="true"
        android:textSize="20dp"
        android:layout_toStartOf="@+id/travel_record_btn" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New Button"
        android:id="@+id/travel_record_btn"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/travel_record_text"
        android:layout_alignBottom="@+id/travel_record_text" />
</RelativeLayout>
